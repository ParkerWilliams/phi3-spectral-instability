# === Build artifacts ===

# Rust / Tauri
target/
**/*.rs.bk
host/src-tauri/target/

# Node / frontend
node_modules/
dist/
.parcel-cache/
.vite/

# Python (sim harness)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Engine build outputs
engine/build/
engine/*.o
engine/fteqw
engine/fteqw-*
quakec/progs.dat
quakec/*.lno

# === Project data ===

# Save state and runtime data
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Sim output (large, regenerable)
sims/results/
sims/*.log

# === Procedural maps (feature 004) ===
.mypy_cache/
.pytest_cache/
.ruff_cache/
gen_*.map
gen_*.bsp
# vendored prebuilt ericw-tools binaries are fetched locally, not committed
tools/ericw-tools/bin/

# Asset symlinks created by setup scripts
assets/_runtime/

# LibreQuake release paks placed locally for headless sim runs (feature 001).
# Runtime data, not source; final vendoring approach is TBD (see docs/licenses.md).
/id1/

# === Local config ===

.env
.env.local
*.local

# === Editor / OS ===

.DS_Store
Thumbs.db
.vscode/
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/
*.swp
*~

# === Session and scratch ===

# Per-developer session logs that aren't worth committing
scratch/
notes-local/
